# build output
/bin/
/loomwork

# local workspace state and real (non-example) configuration
/.loomwork/
config.json
presets.json
!config/*.example.json

# credentials — never committed
.env
.env.*
*.pem

# agent bridge dependencies (installed with `cd bridge && npm install`)
/bridge/node_modules/
/bridge/package-lock.json

# tooling noise
*.test
*.out
.DS_Store
